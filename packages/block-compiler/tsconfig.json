{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist",
    "types": ["node"]
  },
  "include": ["src", "test"],
  // Fixtures are block sources compared byte for byte, not part of this program.
  "exclude": ["test/fixtures"],
  "references": [{ "path": "../contracts" }]
}
